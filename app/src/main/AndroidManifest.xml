<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abc123.my12306">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Ticket.AddUserInfo"></activity>
        <activity android:name=".Ticket.CityActivity">
        </activity>
        <activity android:name=".ViewPagerActivity" />
        <activity android:name=".User.my_detailcontact" />
        <activity android:name=".User.my_pwd" />
        <activity android:name=".User.my_account" />
        <activity android:name=".User.my_contact" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Ticket.Ticketone" />
        <activity android:name=".Ticket.Tickettwo" />
        <activity android:name=".Ticket.CityActivity" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".User.UserActivity" />
    </application>

</manifest>